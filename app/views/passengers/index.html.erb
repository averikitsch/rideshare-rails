<% if @passengers.count == 0 %>
    <h1>There are no passengers to display</h1>
    need to add a link to add some !
<% else %>
    <% @passengers.each do |passenger| %>
         <h2><%= link_to passenger.name, passenger_path(passenger) %></h2>
    <% end %>
<%end%>
